[build-system]
requires = ["setuptools", "wheel", "torch>=2.7.0"]
build-backend = "setuptools.build_meta"

[project]
name = "torch_dpu"
version = "0.1"
license = "BSD-3-Clause"
requires-python = ">=3.8"

[tool.setuptools]
packages = { find = { where = ["."], include = ["torch_dpu*"] } }
